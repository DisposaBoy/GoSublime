sudo: false

language: go

go:
  - 1.11.x
  - 1.12.x
  - tip

matrix:
  fast_finish: true
  allow_failures:
    - go: tip

install: true

script:
  - env GOPATH=$PWD go install -v gosublime/cmd/margo
